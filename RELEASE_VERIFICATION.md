# Release Verification

Verified before upload:

- `run_primitives_check.sh` passed.

Runtime logs, Python bytecode caches, nested Git metadata, virtual environments, build folders, and install folders are intentionally excluded from this repository copy.

`Reshape/yolo26x-seg.pt` is intentionally excluded because it is larger than GitHub's 100 MB per-file limit. Use one of the smaller included weights or download the x model separately if needed.
