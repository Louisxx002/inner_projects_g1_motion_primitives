# Upload Notes

This copy excludes generated caches, nested Git metadata, virtual environments, logs, and build outputs.

`Reshape/yolo26x-seg.pt` is intentionally excluded because it is larger than GitHub's 100 MB per-file limit. Use a smaller included YOLO weight or download the x model separately if needed.
